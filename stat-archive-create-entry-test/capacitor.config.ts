import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'work.lustats.createentrytest',
  appName: 'Create Entry Test',
  webDir: 'dist'
};

export default config;
